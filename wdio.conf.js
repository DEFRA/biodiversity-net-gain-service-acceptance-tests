"use strict";
// wdio.conf.js is the default configuration file used when a specific configuration is not referenced on the command line.
// By default, run with the local configuration
exports.config = require("./src/conf/wdio.local.conf").config;
